{
  "name": "cab-digital-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file, offline, client-side dashboard for the CAB Digital Report workbook.",
  "scripts": {
    "build": "node build.js",
    "test": "node test/parse.test.js && node test/robustness.test.js && npm run build && node test/browser.test.js",
    "test:parse": "node test/parse.test.js",
    "test:robust": "node test/robustness.test.js",
    "test:browser": "node build.js && node test/browser.test.js"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
